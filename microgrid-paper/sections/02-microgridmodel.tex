\section{Distribution-network microgrid and battery-morphology metrics}
\label{sec:system_model}

The networked battery-control setting is a modified IEEE 33-bus feeder under a network-stress regime. The radial feeder includes distributed photovoltaic injections and time-varying loads, and the battery energy storage system (BESS) is placed at the remote end so that charge and discharge decisions interact with voltage and loading stress. Each achieved battery set-point is evaluated through an AC power-flow settlement layer, which records CNY-equivalent operating objective cost and feasibility diagnostics. The same interface provides the morphology diagnostics used to assess learned storage behavior.

\subsection{Networked battery-control interface}

The test system is a modified IEEE 33-bus radial distribution network, as illustrated in Figure~\ref{fig:ieee33_network}. The network includes distributed photovoltaic injections and time-varying loads, with the battery energy storage system (BESS) placed at the remote end (Bus 33) to interact with voltage and loading stress.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.85\columnwidth]{ieee33_network.pdf}
\caption{IEEE 33-bus distribution network topology with battery storage at Bus 33 and distributed PV injections. The radial feeder configuration creates voltage sensitivity to battery dispatch decisions at the remote end.}
\label{fig:ieee33_network}
\end{figure}

The main engineering parameters are summarized in Table~\ref{tab:ieee33_case_parameters}. The BESS is rated at 0.5\,MW/0.896\,MWh with SOC bounds 0.10--0.90, and the input profiles use 15-min resolution. The nominal IEEE33 profile scaling uses a 4.0\,MW peak-load reference and a 0.45\,MW aggregate PV reference before the network-stress multipliers are applied. In the stress regime, load is increased and PV support is reduced relative to the base profile to make voltage and loading diagnostics active in the validation problem.

\begin{table}[!t]
\centering
\footnotesize
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.08}
\caption{IEEE 33-bus network-stress case parameters used in the reported evaluation.}
\label{tab:ieee33_case_parameters}
\begin{tabularx}{\columnwidth}{lX}
\toprule
Item & Setting \\
\midrule
Network model & pandapower \texttt{case33bw}, radial AC power flow \\
BESS location & Bus 33 \\
BESS rating & 0.5\,MW / 0.896\,MWh \\
SOC bounds & 0.10--0.90 \\
PV locations & Buses 25, 30, and 31 \\
Profile resolution & 15\,min, 35040 steps per 365-day year \\
Base profile scaling & 4.0\,MW load peak; 0.45\,MW aggregate PV peak \\
Network limits & $V_{\min}=0.90$\,p.u.; line-loading limit 100\% \\
\bottomrule
\end{tabularx}
\end{table}

At each time step $t$, the energy management system issues a battery set-point $P_{\mathrm{bess}}^{\mathrm{sp}}(t)$. Positive power denotes discharging and negative power denotes charging. The set-point is mapped to an achieved terminal power $P_{\mathrm{bess}}^{\mathrm{ach}}(t)$ after battery-side clipping, SOC consistency checks, and optional shield correction. Grid exchange is determined by
\begin{equation}
P_{\mathrm{grid}}(t)=P_{\mathrm{load}}(t)-P_{\mathrm{pv}}(t)-P_{\mathrm{bess}}^{\mathrm{ach}}(t),
\label{eq:grid_balance_morphology}
\end{equation}
where $P_{\mathrm{load}}(t)$ and $P_{\mathrm{pv}}(t)$ are prescribed exogenous profiles. The achieved battery action is then written into the pandapower network model, and the resulting voltage, loading, and power-flow status are logged. This interface keeps the learning problem continuous while retaining distribution-network settlement during evaluation.

The battery SOC evolves as
\begin{equation}
\mathrm{SOC}(t+1)=\mathrm{SOC}(t)-\frac{P_{\mathrm{eff}}(t)\Delta t}{E_{\max}},
\label{eq:soc_update_morphology}
\end{equation}
where $E_{\max}$ is the battery energy capacity and $P_{\mathrm{eff}}(t)$ is the effective internal-energy power associated with the achieved terminal action. All experiments use a common scheduling interface, with emphasis on validation and protocol behavior rather than on ranking electrochemical model fidelities. Training uses the simple efficiency-based battery model (\texttt{train\_model=simple}), and evaluation is conducted across four model fidelities---simple EBM, Thevenin loss-only, Thevenin Rint-only, and Thevenin full PBM---to assess cross-fidelity behavior. In the six-seed evaluation, mean objective cost changes from 11.788\,M\textyen{} under the simple EBM to 11.737\,M\textyen{} under the Thevenin full PBM. Here M\textyen{} denotes million CNY-equivalent objective units, not a pure market bill: the accumulated objective includes energy settlement, network-settlement terms, and terminal-inventory penalties. The lower higher-fidelity mean is therefore treated as a settlement-accounting effect: replaying the same checkpoint under loss-aware and resistance-aware battery equations changes the achieved terminal power, SOC integration, and terminal-inventory penalty before the objective is accumulated (see Section~\ref{sec:case_study}).

\subsection{Evaluation objective and reference-normalized recovery}

The reported evaluation objective is the cumulative operating objective, including the case-specific energy and network-settlement terms and the terminal SOC penalty used by the evaluation protocol. Let $J_{\pi}$ denote the controller objective cost, $J_{0}$ the no-storage or baseline cost, and $J_{\mathrm{ref}}$ the replayed oracle reference cost. Costs are reported in CNY-equivalent objective units so that physical penalties and settlement terms share a common accounting scale. The reference is a linear-programming schedule computed with perfect foresight over the evaluation horizon and replayed through the same network-settlement interface. It is used as a normalization anchor and an optimistic reference schedule, not as a global-optimality certificate or a strict performance ceiling. The raw reference-normalized recovery index is
\begin{equation}
r_{\mathrm{raw}}=\frac{J_{0}-J_{\pi}}{J_{0}-J_{\mathrm{ref}}}.
\label{eq:raw_recovery}
\end{equation}
The index places windows on a common scale, but it is not a physical benchmark of optimality. It can exceed unity when the learned controller obtains a lower replayed objective than the chosen reference on a finite evaluation window, for example because the replayed reference is not a perfect finite-horizon optimum under the evaluation penalty accounting. To avoid over-interpreting such cases in the main results, display recovery is defined as
\begin{equation}
r_{\mathrm{disp}}=\min\bigl(1,\max(0,r_{\mathrm{raw}})\bigr).
\label{eq:display_recovery}
\end{equation}
The raw value is preserved in trajectory summaries and source-data tables, while the clipped value is used for main-paper visual and tabular interpretation.

\subsection{Battery morphology diagnostics}

Battery morphology is treated as a primary diagnostic outcome. Here, morphology refers to whether the SOC and action trajectory has an interpretable operational shape, rather than merely a low scalar objective. Mid-band dwell is the fraction of evaluation steps in which SOC remains in the preferred middle band; in the reported evaluation this corresponds to the 0.3--0.7 SOC interval, a range commonly recommended for lithium-ion battery longevity in stationary storage applications to balance cycle depth against calendar aging \cite{xu2018modeling,wang2021degradation}. Boundary parking measures persistent residence near upper or lower SOC limits. Terminal SOC deviation measures whether the controller ends the horizon near the target inventory.

For a trajectory $\tau$, the diagnostic set can be written compactly as a morphology vector
\begin{equation}
\mathcal{M}(\tau)=
\bigl[m_{\mathrm{soc}},m_{\mathrm{act}},m_{\mathrm{net}},m_{\mathrm{sh}}\bigr],
\label{eq:morphology_vector}
\end{equation}
where $m_{\mathrm{soc}}$ collects mid-band residence, boundary parking, and terminal-inventory consistency; $m_{\mathrm{act}}$ collects price-responsive charge and discharge behavior; $m_{\mathrm{net}}$ records infeasible-action dwell and power-flow failures; and $m_{\mathrm{sh}}$ records residual shield correction. This notation is diagnostic rather than a new state variable: it defines the quantities used to evaluate and select checkpoints after trajectories have been generated.

Peak-price discharge and valley-price charge fractions quantify whether learned actions align with the economic role of storage. These metrics are defined using percentile-based time-period classification. Let $\pi(t)$ denote the electricity price at time $t$, and let $\tau_{\mathrm{peak}}$ be the set of time steps in which $\pi(t)$ exceeds the 80th percentile of the evaluation-window price distribution, and $\tau_{\mathrm{valley}}$ the set of steps in which $\pi(t)$ falls below the 20th percentile. The peak-price discharge fraction is
\begin{equation}
f_{\mathrm{peak}} = \frac{1}{|\tau_{\mathrm{peak}}|} \sum_{t \in \tau_{\mathrm{peak}}} \mathbb{1}\bigl(P_{\mathrm{bess}}^{\mathrm{ach}}(t) > 0\bigr),
\label{eq:peak_discharge_fraction}
\end{equation}
and the valley-price charge fraction is
\begin{equation}
f_{\mathrm{valley}} = \frac{1}{|\tau_{\mathrm{valley}}|} \sum_{t \in \tau_{\mathrm{valley}}} \mathbb{1}\bigl(P_{\mathrm{bess}}^{\mathrm{ach}}(t) < 0\bigr).
\label{eq:valley_charge_fraction}
\end{equation}
A policy with no price awareness would produce $f_{\mathrm{peak}} \approx f_{\mathrm{valley}} \approx 0.5$ (random action direction), whereas a strongly price-responsive policy would approach $f_{\mathrm{peak}} \to 1$ and $f_{\mathrm{valley}} \to 1$. These metrics are interpreted directionally: a high peak-discharge fraction indicates discharge scheduling aligned with high-price periods, whereas a valley-charge fraction close to 50\% indicates limited low-price charging behavior. Because the fractions count action direction rather than action magnitude, they do not distinguish shallow and deep charge--discharge responses. They are therefore reported as coarse morphology diagnostics rather than complete arbitrage-efficiency metrics.

Infeasible-action dwell measures the fraction of raw requested actions that violate the battery feasibility envelope before execution-layer correction, and power-flow failure steps record feeder-solver failures or infeasible network outcomes.

Shield diagnostics are reported separately because a feasible trajectory may depend on an engineering correction layer. The material shield activation fraction measures non-negligible corrections ($|\delta| > 0.05$), strong shield activation measures larger corrections ($|\delta| > 0.20$), and mean shield correction records average magnitude. These metrics separate shielded execution from full constraint internalization by the neural policy. The battery morphology diagnostics comprise: mid-band dwell (0.3--0.7 SOC, higher preferred), boundary parking (lower preferred), terminal SOC deviation (lower), peak-price discharge fraction (higher), valley-price charge fraction (higher), infeasible-action dwell (zero), and power-flow failures (zero).

\subsection{Morphology diagnostic labels}

For interpretation, each protocol-window family is assigned one of three labels. A controller is \emph{clean} when constraint-clean with sustained mid-band residence and low intervention burden; \emph{improved but fragile} when feasible with interpretable behavior but residual infeasible-action or teacher-gap signals; and \emph{problematic} when exhibiting infeasible dwell, boundary parking, or correction-dominated execution. This three-level labeling is intentionally conservative, allowing P4 BalancedGate to document its remaining intervention diagnostics while avoiding reliance on scalar reward alone.
